<Window x:Class="WIMISODriverInjector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="I.D.I.O.T. - Image Driver Integration &amp; Optimization Tool" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        Closing="Window_Closing">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Border Grid.Column="0" 
                Background="{DynamicResource SurfaceBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
            <StackPanel Grid.Row="0">
                <!-- App Title -->
                <StackPanel Margin="20,20,20,30">
                    <TextBlock Text="I.D.I.O.T." 
                               FontSize="40" 
                               FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryBrush}"
                               HorizontalAlignment="Left"/>
                    <TextBlock Text="Image Driver Integration &amp; Optimization Tool" 
                               FontSize="13" 
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               TextWrapping="Wrap"
                               Margin="0,5,0,0"
                               MaxWidth="210"/>
                </StackPanel>

                <!-- Navigation Items -->
                <StackPanel>
                    <Button x:Name="ImageSelectionButton"
                            Content="1. Image Selection"
                            Click="NavigationButton_Click"
                            Tag="Image"
                            Style="{StaticResource NavigationButtonStyle}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"/>
                    <Button x:Name="DriverSelectionButton"
                            Content="2. Driver Selection"
                            Click="NavigationButton_Click"
                            Tag="Driver"
                            Style="{StaticResource NavigationButtonStyle}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"/>
                    <Button x:Name="OptionsButton"
                            Content="3. Options"
                            Click="NavigationButton_Click"
                            Tag="Options"
                            Style="{StaticResource NavigationButtonStyle}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"/>
                </StackPanel>
                
            </StackPanel>
            
            <!-- Bottom Navigation Items -->
            <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Margin="0,0,0,20">
                <Button x:Name="LogsButton"
                        Content="Logs"
                        Click="NavigationButton_Click"
                        Tag="Logs"
                        Style="{StaticResource NavigationButtonStyle}"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Padding="20,12"/>
                <Button x:Name="AboutButton"
                        Content="About"
                        Click="NavigationButton_Click"
                        Tag="About"
                        Style="{StaticResource NavigationButtonStyle}"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Padding="20,12"/>
            </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" Margin="30">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Style="{StaticResource ModernScrollViewerStyle}">
                <StackPanel>
                    <!-- Image Selection Section -->
                    <StackPanel x:Name="ImageSelectionPanel" Visibility="Visible">
                <TextBlock Text="Image Selection" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,20"/>
                <TextBlock Text="Select an ISO or WIM file to process" 
                           FontSize="14" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,0,0,30"/>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Scratch Drive" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,8"/>
                    <ComboBox x:Name="ScratchDriveComboBox"
                              Style="{StaticResource ComboBoxStyle}"
                              Height="40"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Input File" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="InputFileTextBox" 
                                 Grid.Column="0" 
                                 Margin="0,0,10,0"
                                 IsReadOnly="True"
                                 Style="{StaticResource TextBoxStyle}"/>
                        <Button Grid.Column="1" 
                                Content="Browse..." 
                                Click="BrowseInputFile_Click"
                                Style="{StaticResource ButtonStyle}"
                                Width="100"/>
                    </Grid>
                </StackPanel>

                <!-- ISO Extraction Progress (shown when extracting) -->
                <StackPanel x:Name="ExtractionProgressPanel" Visibility="Collapsed" Margin="0,20,0,0">
                    <TextBlock Text="Extracting ISO file..." 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,10"/>
                    <ProgressBar x:Name="ExtractionProgressBar" 
                                  Height="25" 
                                  IsIndeterminate="True"
                                  Style="{StaticResource ProgressBarStyle}"/>
                    <TextBlock x:Name="ExtractionStatusText" 
                               Text="Please wait..." 
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,10,0,0"/>
                </StackPanel>

                <!-- WIM Files Found (shown after extraction or when WIM selected) -->
                <StackPanel x:Name="WimFilesPanel" Visibility="Collapsed" Margin="0,20,0,0">
                    <TextBlock Text="WIM Files Found" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,10"/>
                    <ListBox x:Name="WimFilesListBox" 
                             Height="150"
                             Style="{StaticResource ListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="5"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <!-- OS Version Selection (shown when install.wim is found) -->
                <StackPanel x:Name="VersionSelectionPanel" Visibility="Collapsed" Margin="0,20,0,0">
                    <TextBlock Text="OS Version Selection" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="Select which Windows versions to include in the final image" 
                               FontSize="12" 
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,0,0,10"/>
                    <ListBox x:Name="VersionListBox" 
                             Height="200"
                             SelectionMode="Multiple"
                             Style="{StaticResource ListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Display}" 
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          Margin="5"
                                          Foreground="{DynamicResource PrimaryTextBrush}"
                                          Click="VersionCheckBox_Click"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <TextBlock Text="Note: All versions in boot.wim will be included automatically" 
                               FontSize="11" 
                               FontStyle="Italic"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Driver Selection Section -->
            <StackPanel x:Name="DriverSelectionPanel" Visibility="Collapsed">
                <TextBlock Text="Driver Selection" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,20"/>
                <TextBlock Text="Select directories containing driver files (.inf) to inject" 
                           FontSize="14" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,0,0,30"/>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Driver Directories" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,8"/>
                    <ListBox x:Name="DriverDirectoriesListBox" 
                             Height="300"
                             Margin="0,0,0,10"
                             Style="{StaticResource ListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="5"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Add Directory..." 
                                Click="AddDriverDirectory_Click"
                                Style="{StaticResource ButtonStyle}"
                                Margin="0,0,10,0"/>
                        <Button Content="Remove Selected" 
                                Click="RemoveDriverDirectory_Click"
                                Style="{StaticResource ButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <!-- Options Section -->
            <StackPanel x:Name="OptionsPanel" Visibility="Collapsed">
                <TextBlock Text="Options" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,20"/>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Output File" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="OutputFileTextBox" 
                                 Grid.Column="0" 
                                 Margin="0,0,10,0"
                                 Style="{StaticResource TextBoxStyle}"/>
                        <Button Grid.Column="1" 
                                Content="Browse..." 
                                Click="BrowseOutputFile_Click"
                                Style="{StaticResource ButtonStyle}"
                                Width="100"/>
                    </Grid>
                </StackPanel>

                <StackPanel Margin="0,0,0,20">
                    <CheckBox x:Name="OptimizeCheckBox" 
                              Content="Optimize and shrink WIM file after injection"
                              IsChecked="True"
                              Margin="0,5"
                              Style="{StaticResource CheckBoxStyle}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Log File" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="LogFileTextBox" 
                                 Grid.Column="0" 
                                 Margin="0,0,10,0"
                                 Text="injection-log.txt"
                                 Style="{StaticResource TextBoxStyle}"/>
                        <Button Grid.Column="1" 
                                Content="Browse..." 
                                Click="BrowseLogFile_Click"
                                Style="{StaticResource ButtonStyle}"
                                Width="100"/>
                    </Grid>
                </StackPanel>

                <!-- Progress Section -->
                <StackPanel Margin="0,30,0,0">
                    <TextBlock Text="Progress" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               Margin="0,0,0,8"/>
                    <ProgressBar x:Name="ProgressBar" 
                                  Height="25" 
                                  Margin="0,0,0,10"
                                  Style="{StaticResource ProgressBarStyle}"/>
                    <TextBlock x:Name="StatusTextBlock" 
                               Text="Ready" 
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>

                <!-- Start Processing Button -->
                <Button x:Name="StartProcessingButton"
                        Content="SQUIRT!" 
                        Click="StartProcessing_Click"
                        Style="{StaticResource PrimaryButtonStyle}"
                        FontSize="28"
                        Width="200"
                        Height="70"
                        Margin="0,30,0,0"
                        HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- Logs Section -->
            <StackPanel x:Name="LogsPanel" Visibility="Collapsed">
                <TextBlock Text="Logs" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,20"/>
                <TextBlock Text="View and manage log files" 
                           FontSize="14" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,0,0,30"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="Available Logs" 
                                   FontSize="14" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   Margin="0,0,0,10"/>
                        <ListBox x:Name="LogFilesListBox" 
                                 Height="400"
                                 SelectionChanged="LogFilesListBox_SelectionChanged"
                                 Style="{StaticResource ListBoxStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Margin="5"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Log Content" 
                                   FontSize="14" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   Margin="0,0,0,10"/>
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto"
                                      Style="{StaticResource ModernScrollViewerStyle}"
                                      Height="400">
                            <TextBlock x:Name="LogContentTextBlock" 
                                       TextWrapping="Wrap"
                                       Style="{StaticResource LogTextBoxStyle}"
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       Background="#1E1E1E"
                                       Foreground="#D4D4D4"
                                       Padding="8"/>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- About Section -->
            <StackPanel x:Name="AboutPanel" Visibility="Collapsed">
                <TextBlock Text="About" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,20"/>
                <TextBlock Text="I.D.I.O.T." 
                           FontSize="20" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,5"/>
                <TextBlock Text="Image Driver Integration &amp; Optimization Tool" 
                           FontSize="14" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,0,0,30"/>
                <TextBlock Text="A portable tool for injecting drivers into Windows installer images." 
                           FontSize="14" 
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,20"/>
                <TextBlock Text="Features:" 
                           FontSize="14" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,10"/>
                <TextBlock Text="• Inject drivers into Windows installer ISO/WIM files&#x0a;• Select specific Windows versions to include&#x0a;• Optimize and shrink WIM files&#x0a;• Modern Windows 11 interface&#x0a;• Portable and self-contained" 
                           FontSize="12" 
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="20,0,0,0"/>
            </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
